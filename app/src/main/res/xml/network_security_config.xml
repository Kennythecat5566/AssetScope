<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- The client additionally rejects cleartext URLs outside private LAN ranges. -->
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
